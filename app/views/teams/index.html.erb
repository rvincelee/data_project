<h1>All Teams</h1>

<%= link_to('Back to Home', {controller: 'home', action: 'index'}) %>

<ul>
  <% @teams.each do |team| %>
  <li>
    <a href="/teams/<%= team.id %>">
      <%= team.name %>
    </a>
  </li>
  <% end %>
</ul>